import React, { useContext } from "react";
import { GreetContext } from "../Home/Home";

const OrderReview = () => {
  // const greeting = useContext(GreetContext);
  return (
    <div>
      <h1>This page is about order reviews</h1>
      {/* {greeting} */}
    </div>
  );
};

export default OrderReview;
